/** @type {import('next').NextConfig} */
const nextConfig = {
  // This file exists only to help Vercel detect this as a Next.js project
  // The actual configuration is in fashun-store/next.config.js
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig