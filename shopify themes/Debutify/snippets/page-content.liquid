<header class="section-header text-center">
  <h1 class="page-title">{{ page.title }}</h1>
</header>

{% unless page.content == blank %}
  <div class="rte">
    {{ page.content }}
  </div>
{% endunless %}