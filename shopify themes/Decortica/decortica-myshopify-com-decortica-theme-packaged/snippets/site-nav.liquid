<ul class="site-nav">
  {% for link in linklists.main-menu.links %}
  	
    {% if settings.display_megamenu %}
      {% assign is_megamenu = false %}
      {% for i in (1..10) %}
        {% capture megamenu_item %}megamenu_item-{{ i }}{% endcapture %}
        {% if settings[megamenu_item] == link.handle %}
          {% assign is_megamenu = true %}
          {% assign indexOf = i %}
        {% endif %}
      {% endfor %}
    {% endif %}
  
    <li {% if linklists[link.handle] != empty %} class="dropdown {% if is_megamenu %} mega-menu{% endif %}"{% endif %}>
      <a href="{{ link.url }}" {% if link.active %}class="current"{% endif %}>
        <span>
          {% include 'multilang' with link.title %}
        </span>
        {% if linklists[link.handle] != empty and settings.show_dropdown_arrow %}<span class="icon-dropdown"></span>{% endif %}
      </a>
      {% if linklists[link.handle] != empty %}
      	{% assign link_child = link.handle %}
        {% if is_megamenu %}
          <div class="site-nav-dropdown">
          {% include 'mega-menu' with link_child %}
          </div>
        {% else %}
          {% include 'dropdown-menu' with link_child %}
        {% endif %}
      {% endif %}
    </li>
  {% endfor %}
</ul>