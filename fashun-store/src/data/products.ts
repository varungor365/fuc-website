// Export products from mockProducts for compatibility
export * from './mockProducts';
export { mockProducts as products, mockProducts as default } from './mockProducts';

// Export categories from config
export { productCategories as categories } from '../config/products';